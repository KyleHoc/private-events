<!DOCTYPE html>
<html>
  <head>
    <title>PrivateEvents</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <style type="text/css">
      .index_link {color: yellow; font-size: 32px; text-decoration: none}
      .header_bar {background-color: blueviolet; width: 100%; height:15%; margin-top: -15px; display: flex; justify-content: space-between}
      .navbar-link {color: yellow; text-decoration: none}
      .topbar {font-size: 20px; display:flex; align-items: center; padding-right: 5px; color: yellow}
      .navbar-text {display: block}
      .navbar-link {align-items: center; color: yellow}
    </style>
  </head>

  <body>
    <p class="navbar-text float-right">
    <div class="header_bar">
      <%= link_to "Private Events", root_path, :class => "index_link" %>
      <% if user_signed_in? %>
      <div class="topbar">
        Logged in as <strong><%= current_user.email %></strong>.
        <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %>  | 
        <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
      </div>
    <% else %>
      <div class = "topbar">
        <%= link_to "Sign up", new_user_registration_path, :class => 'navbar-link'  %>  | 
        <%= link_to "Login", new_user_session_path, :class => 'navbar-link'  %>
      </div>
    <% end %>


    </div>
    
    </p>

    <% if notice %>
      <p class="alert alert-success"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-danger"><%= alert %></p>
    <% end %>
    <%= yield %>
  </body>
</html>